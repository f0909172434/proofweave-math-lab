# This artifact layer is intentionally request/ledger-only; it cannot operate a
# workspace or retry a paid provider request.
sensitivity: private
enabled: auto
rollout_state: pilot_only
daily_auto_routing: false
default_sensitivity: private
spend_authorization: existing_balance
spend_mode: existing_balance
maximum_usd_per_job: 0.70
maximum_cny_per_job: 5.00
deepseek_cap_usd: 0.70
deepseek_cap_cny: 5.00
retry_on_402: false
workspace_operations:
  enabled: false
workspace_operations_mode: disabled
fallback: proofweave_native
console_verified_is_rejected: true
raw_chain_of_thought_is_rejected: true
raw_chain_of_thought: forbidden
unknown_family_independent_reviewer_is_rejected: true

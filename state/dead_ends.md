# Dead ends and failed routes

## 2026-07-24

- A combined passive-probe command timed out; the probes were split into bounded commands and completed.
- `latexmk` failed because MiKTeX could not find Perl. Direct two-pass `pdflatex` compilation succeeded and became the supported path.

Do not delete failed routes. Add cause, evidence, consequence and whether retry is justified.

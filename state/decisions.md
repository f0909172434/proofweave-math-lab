# Decisions

## 2026-07-24

- Use standard-library Python so validation works without installing packages.
- Treat the fact graph as the only formal truth source; keep memory and worker scratch separate.
- Reserve promotion for an independent cold-start theorem verifier and enforce it in code.
- Model current host-native capability as MODE A; keep CLI/API/gateway execution disabled.
- Borrow Danus principles but neither copy nor install Danus.
- Use direct `pdflatex` because the local `latexmk` wrapper lacks Perl.
- Keep benchmark rankings unavailable until independent keys and sufficient samples exist.

name = "model_router"
description = "ProofWeave model_router adapter; canonical contract is agents/model_router.md."
developer_instructions = """
Before acting, read agents/model_router.md from the repository root in full and follow it together with its mandatory shared-contract references. Do not weaken, duplicate, or rewrite the canonical contracts.
"""

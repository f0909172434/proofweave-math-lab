---
name: route-task
description: Run the ProofWeave route-task workflow using the canonical project artifact.
---

Read `workflows/16_model_routing.md` in full, then follow it. Canonical role contracts remain in `agents/`; truth-layer changes must go through deterministic CLI gates. Do not perform paid calls, publish, push, upload, or expose credentials without explicit authorization.

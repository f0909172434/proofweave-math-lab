---
name: model-doctor
description: Run the ProofWeave model-doctor workflow using the canonical project artifact.
---

Read `docs/model_detection_limits.md` in full, then follow it. Canonical role contracts remain in `agents/`; truth-layer changes must go through deterministic CLI gates. Do not perform paid calls, publish, push, upload, or expose credentials without explicit authorization.

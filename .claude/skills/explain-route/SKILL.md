---
name: explain-route
description: Run the ProofWeave explain-route workflow using the canonical project artifact.
---

Read `docs/model_routing_guide.md` in full, then follow it. Canonical role contracts remain in `agents/`; truth-layer changes must go through deterministic CLI gates. Do not perform paid calls, publish, push, upload, or expose credentials without explicit authorization.

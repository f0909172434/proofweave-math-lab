# Copy to .env only when an explicitly authorized provider workflow needs it.
# Never commit .env or place secret values in command-line arguments or logs.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_COMPATIBLE_BASE_URL=
MATHLAB_LLM_GATEWAY_URL=


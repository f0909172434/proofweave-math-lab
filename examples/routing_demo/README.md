# Dry routing demonstrations

These six tasks exercise classification and model/effort recommendation without
calling any model or provider. Run:

```powershell
python scripts/run_routing_demo.py
```

Results include complexity, risk, selected capability tier, requested/effective
reasoning and rationale. Model performance rankings remain unavailable until an
independently keyed benchmark has sufficient data.

